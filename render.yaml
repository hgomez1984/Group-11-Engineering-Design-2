services:
  - type: web
    name: dental-ai-app
    env: python
    region: florida
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn server:app --host 0.0.0.0 --port $PORT"
